# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database Configuration
# Use the SAME database as your Minecraft server
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=s99457_VaeBridge

# Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. The database must be the SAME one used by TheBridge plugin
# 3. Make sure your database server allows remote connections if hosting remotely
# 4. Never commit the .env file to version control
